package shapesview;

import java.awt.*;

/**
 * Interface for BodyPanel.
 */
public interface IBodyPanel {

  /**
   * Method to paint the components on the body panel.
   * @param g the <code>Graphics</code> object to protect
   */
  void paintComponent(Graphics g);
}
